# Google Maps Platform API key.
# Enable: Places API (New), Geocoding API.
# If left empty, the app runs in MOCK mode with realistic fake data so you can
# exercise the full flow without any credentials.
GOOGLE_MAPS_API_KEY=

# Force mock data even if a key is present (useful for offline dev / demos).
USE_MOCK_DATA=false
